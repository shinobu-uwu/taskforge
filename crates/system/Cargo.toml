[package]
name = "system"
version.workspace = true
edition.workspace = true

[dependencies]
sysinfo.workspace = true

[target.'cfg(windows)'.dependencies]
windows = { workspace = true, features = ["Win32_System_SystemInformation", "Win32_System_ProcessStatus", "Win32_System_Power"] }
